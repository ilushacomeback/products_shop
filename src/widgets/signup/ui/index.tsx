import { SignupForm } from '../../../features';
import { CustomMain } from '../../../shared/ui-kit';

export const Registration = () => {
  return (
    <CustomMain>
      <SignupForm />
    </CustomMain>
  );
};
